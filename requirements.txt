# Core web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Config & validation — pinned to versions with broad wheel coverage
# pydantic 2.10.6 has pre-built wheels for CPython 3.9-3.13 on Linux (manylinux + musllinux)
pydantic==2.10.6
pydantic-core==2.27.2
pydantic-settings==2.7.1

# HTTP client for WhatsApp Cloud API + Dell AI Factory (NIM / OpenAI-compatible) calls
httpx==0.28.1

# Form parsing (USSD aggregators post application/x-www-form-urlencoded)
python-multipart==0.0.20

# Async SQLite for the analytics event store (Phase 2)
aiosqlite==0.20.0

# Document extraction for PDF + Word uploads (past papers, worksheets, homework).
# Both are pure-Python (no native deps, no cargo) — safe on Render's free tier.
pypdf==5.1.0
python-docx==1.1.2

# --- Dev / test ---
pytest==8.3.4
